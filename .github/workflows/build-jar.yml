name: Build Jar File

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-jar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # Berechtigung für gradlew setzen
      - name: Grant execute permission for Gradle
        run: chmod +x gradlew

      - name: Build Jar File
        run: ./gradlew build

      - name: Upload Jar Artifact
        uses: actions/upload-artifact@v3
        with:
          name: jar-file
          path: build/libs/*.jar
